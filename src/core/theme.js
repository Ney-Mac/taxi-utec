import { DefaultTheme } from 'react-native-paper'

export const theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    primary: '#2EB0D9',
    secondary: '#56C7F0',
    error: '#dc242c',
    warn: '#FFEA00',
  },
}
